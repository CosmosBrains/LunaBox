import { HSLA, RGBA } from "./types";
export declare const normalizeColor: (value: unknown) => string;
export declare const normalizeColorStr: (str: string) => string;
export declare const parseColor: (color: unknown, opacity?: boolean) => [HSLA, RGBA | undefined];
